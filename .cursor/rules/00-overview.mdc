---
description: ChinaTechOS 项目总览与设计强约束（红线）
alwaysApply: true
---

# ChinaTechOS · 项目总览

维修工单后台管理系统。技术栈：**Next.js 16 App Router + React 19 + Tailwind v4 + shadcn/ui + framer-motion**。设计语言：**深色玻璃拟物 + 紫青品牌渐变 + 富微交互**。

## 红线（违反即视为 Bug）

1. **不写死颜色**。禁止 `text-white`、`bg-black`、任意 hex/rgb，统一走 `apps/backoffice/src/app/globals.css` 中的语义 token（`bg-card text-card-foreground` 等）。
2. **侧栏与移动 Sheet 必须实色**。禁止在侧栏上使用 `backdrop-blur*` 或带 alpha 的背景；`box-shadow` 可带 alpha。
3. **品牌渐变只用一种**：`var(--gradient-brand)`（紫 → 青）。不另造紫青组合。
4. **字体只用三套**：`font-sans` (Inter) / `font-display` (Space Grotesk) / `font-mono` (JetBrains Mono)。标题用 `font-display`，数字用 `tabular-nums` 或 `font-mono`。
5. **默认深色主题**。亮色仅作切换适配，不得新增第三套主题或改变品牌色调。
6. **不修改** `src/components/ui/*` 中的 shadcn 原子组件。需要变体时通过 `cva` 在调用处组合，或在原文件加新 `variant`。
7. **不引入** `react-router-dom`、路由统一 `next/navigation`。
8. **不编辑** 自动生成的文件。

## 项目结构速记

```
apps/backoffice/src/
  app/                 Next.js App Router 页面
    (app)/             应用壳（含侧栏 + AppBar）
    (auth)/            登录等认证页面
    api/               API Routes
  components/
    ui/                shadcn 原子组件（勿改）
    AppShell.tsx        应用壳
    Sidebar.tsx         侧栏
    TopBar.tsx          顶部条（AppBar）
    background-orbs.tsx 背景渐变球
    command-palette.tsx 全局 ⌘K
    theme-toggle.tsx    亮/暗切换
    route-transition.tsx 页面切换动画
  lib/
    motion.ts           动画 variants 单一来源
    utils.ts            cn() 工具
    data/               数据访问层
    domain/             领域逻辑
    mock/               mock 数据
  app/globals.css       所有设计 token 的唯一来源
```

## 工作流

修改 UI 之前先读：`10-design-tokens.mdc`（颜色 token）、`20-layout-shell.mdc`（外壳结构）、`40-page-recipes.mdc`（页面骨架）。
