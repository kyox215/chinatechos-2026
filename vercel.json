{
  "framework": "nextjs",
  "installCommand": "npm install && npm install --prefix apps/backoffice",
  "buildCommand": "npm run build --prefix apps/backoffice && rm -rf .next && cp -R apps/backoffice/.next .next"
}
